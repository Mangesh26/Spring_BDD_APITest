package com.example.Action;

import java.io.FileReader;
import java.io.IOException;
import java.util.Properties;

public class Inilization {

	public void inilizeFile() throws IOException {
		/*
		 * String path = System.getProperty("user.dir"); path =
		 * path+"\\src\\main\\resources\\application.properties"; FileReader reader=new
		 * FileReader(path);
		 * 
		 * Properties p=new Properties(); p.load(reader);
		 * 
		 * System.out.println(p.getProperty("GET_URL"));
		 * System.out.println(p.getProperty("USER_AGENT"));
		 */
		
		System.out.println("Calling");
	}
}
